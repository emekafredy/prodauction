<div class="h-screen mb-48">
  <div class="mt-48 w-[95%] md:w-[650px] lg:w-[800px] m-auto border-b border-b-purple-300 flex items-center justify-between">
    <h1 class="text-lg sm:text-2xl font-bold">Edit Item</h1>
    <%= link_to "Back to items", items_path, class: "text-sm sm:text-md text-purple-300 underline" %>
  </div>

  <%= render "form", item: @item, mode: @mode %>
</div>
